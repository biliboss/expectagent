- run: 2026-08-19T21:36Z
  verdict: FAILED
  target: process
  cases:
  - looks_up_before_answering:
    - user: ping
    - tool: knowledge.search
      args:
        name: ping
    - agent:
        contains:
          all:
          - pong
          none:
          - 🏓
  - order_is_the_assertion:
    - user: ping out of order
    - tool: knowledge.search
      args:
        name: ping
      happened: expected knowledge.search(ping), got whatsapp.send(pong)
  - reply_may_not_promise:
    - user: ping with flourish
    - tool: knowledge.search
      args:
        name: ping
    - agent:
        contains:
          none:
          - 🏓
      happened: said '🏓', which it must not
